1. 设置blockage但是没注意partical blockage没有写入正确的percent二而设成了100

不想再跑floorplan，在tune里面重新设置

```tcl
set blks [get_placement_blockages SPLIT_BLK_3* -quiet]
if {[sizeof_collection $blks]} {
    set_attribute $blks blockage_type partial
    set_attribute $blks blocked_percentage 70
}
```

